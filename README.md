Just storing this here so I can easily recreate my setup anywhere. Requires nvchad with no default config set up first. 

Probaby requires a few dependencies such as latest git, npm, maybe more.
Should hopefully be clear what the issue is from any errors
